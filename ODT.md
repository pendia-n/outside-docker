# Outdock T — 证据轨道产品方案（F < T）

状态：**新产品提案，不是已实现功能或对旧 OD 方案的覆盖**。编写日期：2026-09-24。旧版产品、收费、Track H/M、Verifier 与链上设计仍保存在 `membrane/`；本文件专门探索「Tap（可验证的实际效用）大于 Fountain（体验本身）」的新方向。这里的 T 不是让界面难用，而是把可信、快速、可互操作的证据交付放在视觉沉浸感之前。

## 1. 一句话与最小市场切口

**Outdock T 是跨组织事实交接的证据轨道**：运营组织把业务事件与原始材料的指纹持续写入有序日志；Outdock 立即给签名收据，批量锚定公开链；被邀请的另一方无需购买或信任运营组织的后台，即可核对时间、版本、顺序、材料匹配和锚定状态。

先做一条具体的水平能力、一个垂直入口：**物流/履约交接**。例子是供应商、承运商、收货方、保险核查员对一单配送中的接单、装车、交付、签收、事故报告有不同记录。Outdock 让他们共享「哪些证据在何时提交、后来有无更正、现在能否复核」，而不是替代运单、ERP、保险系统。验证此切口后才扩展到设备维护、施工验收、供应链质检、托管交易、医疗/保险资料交接等；每个领域需要不同的模板、权限与合规审查。不能把这些扩展当成已验证需求。

### 可验证的承诺 / 不能承诺的事

Outdock 能证明：某些字节的摘要与元数据在 Outdock 接收时已被承诺；平台签署过对应收据；同一轨道的记录按位置相连；某一批记录的 Merkle 根在特定链交易确认后被锚定；获授权的验证者拿到原始材料和披露盐后可重新计算并匹配。链上保存批次根，不保存原文、逐条记录、客户身份或敏感元数据。

它**不能**证明：提交内容是真的、设备没有说谎、原件在平台接收前就存在、一个孤立记录代表完整历史、一个组织没有在别处另建轨道、法院必然采纳。`H = SHA-256(规范化后的确切字节)`；`C = SHA-256(域分隔符 || 每记录随机盐 || H)`。**H/C 都不能反向解哈希**。验证只能拿原件重新计算 H、再用披露盐重新计算 C。原件或盐缺失时，要清楚显示“承诺/时间/链可核；内容匹配未核”，不能显示总的“已证实”。

## 2. 角色、权限与付费原则

不再把 H/M 当成同时展示给所有人的三种账户身份。**账户属于组织，权限属于成员，写入渠道属于证据源**。同一组织可有网页人工源与 API 自动源；一个人只看到被授权的工作区。

| 角色 | 主要工作 | 网页 | API | 边界 |
|---|---|---|---|---|
| 组织所有者 / 管理员 | 建组织、项目、证据类型、成员、来源、分享政策和账单 | 全部配置与审计 | 管理 API（高权限、可关闭） | 不能改写既有证据 |
| 人工提交者 | 新建事项/交接、上传或录入证据、追加更正、请求披露 | 写入 + 查看 | 可用受限写入 API | 不能删除/重写历史 |
| 系统集成者 | 将 ERP、设备、仓储等系统接入 | 建源、发/撤销 API 凭证、看失败队列 | 幂等批量提交、状态查询、Webhook 管理 | 凭证按组织/源/动作限定 |
| 内部审查员 | 检查缺口、迟到、冲突、锚定失败 | 只读、标记跟进 | 只读 API 可选 | 不得伪装独立第三方 |
| 外部验证者 | 接受邀请，查看授权范围，提出材料披露请求，独立复核 | 完整验证工作流 | 验证/只读 API 可选；机器对机器复核可做后续阶段 | 看不到未授权原件/其他事项 |
| 平台运营 | 处理异常、账务与滥用 | 受审计的有限后台 | 运维 API | 不能悄悄变更签名历史；访问客户材料需单独授权 |

**ODT 目前没有定下美元标价，也没有已实现的收费计划。**以下是建议验证的收费结构，不是已经存在的 Stripe 产品或已确认价格。建议由持有证据轨道的组织付费，受邀验证者免费获得该次事项的授权访问；否则每次交接都要求收货人先买单，会给证据传递增加阻力。

| ODT 计划提案 | 付费方式 | 包含什么 / 对应权限 | 适合谁 |
|---|---|---|---|
| **单事项包** | 一次性，按一个有明确上限的 Case 收费 | 一个组织的一个事项；网页人工提交 + API 来源；标准批次锚定；邀请所需对方人数不另收费；事项开放期内可持续追加记录，结案后保留只读核验一段明确期限。超过记录量或归档期时在购买前说明加购条件 | 偶发的一次交付、验收或争议留证；不应按验证者选的日期区间收费 |
| **组织订阅** | 月/年订阅，价格按用量档验证 | 持续创建事项与事件；人工网页 + API；成员/来源管理；标准定时锚定、基础异常提醒、固定保留期；同一事项的外部验证者包含在组织权益中。档位主要按月事件量、活跃事项、保留期和 API 吞吐区分，不按验证者席位收费 | 每周持续产生交接记录的运营团队 |
| **高量集成订阅** | 月/年企业合同 | 组织订阅全部能力，加较高吞吐、多个项目/来源、定制保留期、优先锚定额度、Webhook SLA、审计导出/支持；具体额度与链上费用单列写清楚 | ERP/设备持续写入、需要有响应 SLA 的公司 |

这三种是待验证的产品包，不是三个已存在产品。**第一阶段只需测试“单事项包 + 一个组织订阅档”**，高量合同等出现真实客户需求再报价。价格数字暂不填：目前没有 ODT 客户访谈、成本曲线或付费转化数据；现在填 $X 会是假精确。可在试点中让同类客户对单事项固定费和组织月费做真实报价测试，记录接受、拒绝及原因，再定价格与额度。组织可对特定事项加购优先锚定或加密托管；普通锚定应批处理并包含在额度中，托管按存储量和保留期计费。

**一次性收费与订阅不是互斥选项**：ODT 应保留单事项的一次性购买，给低频组织免去长期订阅；持续运营的组织使用订阅，外部验证者对被邀请事项免费。单事项包的授权绑定 Case 和结案/保留期限，结案前可查看后续追加的相关事件；这和旧 OD 的“买一段历史时间窗、后续新数据不自动包含”不同，也和“付一次钱永久读取供应商所有事件”不同。

### 角色如何承接旧 OD

| 旧 OD 概念 | ODT 中对应角色/功能 | ODT 权限与付费归属 |
|---|---|---|
| Supplier / Track H | 组织 + 人工提交者 | 组织所有者购买单事项包或订阅；提交者按成员权限用网页和受限 API 写入 |
| Supplier / Track M | 组织 + 系统集成者 / Source | 同一个组织订阅；集成者管理来源凭证并通过 API 写入，网页用于配置、读历史、看错误，不提供机器记录手工写入表单 |
| Verifier | 外部验证者 | 不单独订阅，不为每个 7 天日期块付款；受邀后免费核验授权事项，并按字段/材料走披露审批 |
| Supplier 团队内审查 | 内部审查员 | 组织订阅所含成员权限；只读检查缺口、迟到、锚定和争议，不替代外部独立验证 |
| 组织负责人 | 所有者 / 管理员 | 负责购买计划、成员和访问策略；能配置但不能改写历史 |

同一个组织可同时有人工提交者和 API 集成者；它们不是要再买两份“Track H/M 账号”。是否按成员收费应在试点后决定，初始方案不按被邀请验证者数量收费，避免客户因怕增加对方账号费用而不邀请核验人。

## 2.1 旧 OD 收费和 ODT 提案不能混成一套

旧方案的历史模型在 `membrane/03-TRACK-H-AND-TRACK-M.md` 和 `membrane/05-VERIFIER-ACCESS-AND-BILLING.md` 有记录：Supplier A/B/C/D 的存档价格为 `$99/$299/$799/$1,999`，差异按写入速率和批量上限；D1 没有明确计费周期字段，周期由 Stripe Price 配置决定，而且文档记录了生产注册没有完整接通付款。Verifier 旧模式为：一次性买一个明确日期区间，按每个开始的 7 天单位 `$25`；同一笔购买达到 7 个单位时，整笔总价打五折（即 `$12.50 × 全部单位`）；另有 `$88 / 28 天` 订阅，包含付款时刻前 30 天历史和订阅期间持续新增的相关数据。旧代码的 `$29 / 30 天 Read Pass` 是另一套遗留路径，不应当作同一产品叠加。

这些是**旧 OD 的记录，不是 ODT 的价格建议**。ODT 不应原样复制 A–D 和 Verifier `$25/$88`：旧模型让供应商与验证者两边分别付费，并把 Verifier 访问切成时间窗口；ODT 的价值单位是持续运行的一条业务事项轨道，邀请方希望对方能把同一 Case 的完整证据链核完。建议把付费集中到提供记录和运营这条轨道的组织，同时为低频客户保留按 Case 的一次性收费。若未来发现验证者是明确的预算所有者，可以单独做行业试验，但不把它设成默认门槛。

## 3. 核心领域模型

- **Organization**：付款、成员、政策与数据所属主体；有独立环境与账本。
- **Workspace/Project**：合作关系或业务范围，例如 Florence 冷链交付。
- **Evidence type**：模板，如签收、温度异常、事故；字段版本化，旧记录保留旧版本。
- **Case/Transaction**：一个具体可核查事项，如配送单 `DEL-2026-0421`，有明确开始/完成/争议状态。
- **Source**：人工浏览器、ERP 集成、设备、外部系统；每个源有身份、密钥、序号策略和健康状态。
- **Event**：不可变的一个事实声明，带发生时间（提交方声称）、接收时间（平台观测）、源、动作、结构化上下文摘要、内容承诺 C、前一事件证明、签名收据和锚定投影。
- **Correction/Supersession**：新事件指向旧事件并说明原因；不物理覆盖。
- **Disclosure package**：可选、独立授权的原件或字段、随机盐、规范化规则、收据和 Merkle 路径。默认不保存原件；若客户购买托管，加密后在 R2 保存，密钥与访问审批分离。
- **Access grant**：组织、事项/类型、时间窗、字段和有效期的授权；撤销控制未来读取，不会收回已被看到的内容。
- **Anchor batch**：批次根、叶子版本、链 ID、合约地址、交易哈希、确认数、状态和失败信息。
- **Audit event**：谁申请、批准、查看、导出/披露、撤销；不可仅靠 UI 隐藏来宣称不能复制。

关键关系：`组织 → 项目 → 事项 → 有序事件`；`证据类型`分类事件，`来源`标识写入者；`授权`切出验证者可见的视图；`批次`跨事项聚合上链但链上不暴露事项。事件链不能只按“配送类型”串 1 万条而丢失每单语境；每个事项有自己的顺序，同时可按类型横向检索。

## 4. 从开始到完成：各角色流程

### 4.1 组织启动

1. 管理员注册并验证邮箱/组织控制权；完成强认证与恢复方式。用户名可见，但不作为唯一的组织授权因素。
2. 创建组织、项目、事项编号规则和一两个证据类型模板。系统提供示例，但不预设“法律有效”。
3. 选择默认内容政策：**本地指纹、不保留原件**；或显式启用加密托管并设保留期、密钥责任及额外费用。
4. 邀请人工成员，创建系统源；为每个源签发一次可见、可轮换的凭证，仅授予需要的范围。
5. 配置通知对象、异常规则与外部验证者邀请政策。仪表盘首先显示“哪些事项需行动”，而非总事件数。

### 4.2 人工网页提交

1. 选择现有事项或创建新事项（业务外部 ID、对方、时间与证据类型）。
2. 输入文字或选文件（PDF/图片/音频/视频/任意字节）。浏览器展示内容将如何被标准化：文件按原始字节；文字按 UTF-8；JSON 按公布的确定性规范。文件不应被无意转码。
3. 浏览器本地算 H，生成逐条随机盐，算 C 与上下文摘要。明确显示“原件不上传 / 仅承诺上传”，提供上传前摘要预览。
4. 用户确认后发送 C、必要非敏感字段、上下文摘要、幂等键；服务端在该事项的串行通道赋位置、接收时间、前链证明，签收据并原子持久化。
5. 立即显示 **已收据签名 · 待链上确认**；保存可恢复的披露材料/包。若用户关闭页面后丢失本地盐，日后无法做内容匹配；因此 MVP 不得把“下载一次”当可靠交付。应有清楚的恢复策略：客户管理的安全库、可选加密托管，或提交前确认由客户保存披露包；没有恢复路径时阻止误导性的完成文案。
6. 定时或按积压阈值批量上链。界面状态依次为已接收、已签名、待提交、已提交、确认中、已锚定；失败/重试单独标出。
7. 发现错漏时追加更正，指向原事件，不擦除旧版。事项结束后可封存；迟到事件需特殊标签。

### 4.3 自动系统/API 提交

1. 集成者创建 Source、定授权域和序号策略，从 UI 一次性取凭证，存入调用方密钥管理系统。
2. 客户端推荐在自己环境算 H/C，传 `commitment`、`manifest_hash`、发生时间、事项 ID、源序号、幂等键。小体积文字/JSON 可以选择由服务器**瞬时**计算，但文案必须说“会经过服务器，不落地为原件”；大文件不经过 Worker。
3. API 同步返回事件 ID、位置、签名收据、当前锚定状态和可查询地址。批量提交逐条给成功/失败，不把部分成功伪装为全成功；相同幂等键与相同请求重放返回同一结果。
4. API 客户用签名 Webhook 或轮询接收 `anchored`、`retrying`、`source_gap`、`disclosure_requested` 等事件；Webhook 失败有可见重试与死信队列。
5. 集成者在“源健康”页看最近心跳、序号间隙、延迟、拒绝原因、当前锚定积压。机器源没有网页“代替 API 提交”的输入表单，人工源不妨使用同一公开 API 语义。

### 4.4 外部验证者

1. 从事项链接/邀请打开验证页；先看到组织是谁、事项与授权范围、邀请来源、当前独立可核的层级。对私有内容需要接受邀请并登录；无需先支付。
2. 时间线将 **发生时间（声明）/接收时间（平台签名）/链上时间（确认）** 三列分开。每条有版本/更正关系、缺口与迟到标识。
3. 无原件时先核收据签名、事件链、Merkle 路径、指定链合约/交易、批次确认；结果标记“结构与锚定已核，内容未核”。待链上时仅能说“平台已签收”，不可说链上成立。
4. 需要核某张签收图或某份 PDF 时点“请求披露”，选字段/材料、用途、截止时间。组织管理员批准后，可通过安全传递材料/盐/规范化信息；也可在客户现有渠道交付后在浏览器本地装载披露包。验证者用确切原件算 H、用盐算 C，与收据/链上证明逐层对照。
5. 输出的是分层核验结果与引用编号，不是“事实为真”章。验证者可记录异议，组织看到并追加解释/更正。已知的核验结果与审计记录可分享给被明确授权的人；敏感材料默认不可随便公开。
6. 授权到期后阻止新的平台读取，但不能让已经披露的原件或截图“自动消失”。界面与条款如实说明这一点。

## 5. UI / UX 信息架构与主要页面

设计原则：桌面 13 英寸优先，平板可并排压缩，窄手机单列；仅亮色模式。使用清晰的信息层级：业务事项 → 记录顺序 → 证据强度 → 下一动作。颜色只作辅助手段，状态必须有文字。固定顶栏放组织切换和用户设置；左导航只显示权限允许的任务；主栏不塞无关统计。

1. **首页/注册**：一句实用承诺、一张“原件→H→C→签名收据→批次上链→独立重算”图、三步试用，不先用 Web3 术语劝用户。注册先问“我的团队要提交记录 / 我要核验别人给的记录”，不是强迫自称 Track H/M/Verifier；同一邮箱可被不同组织邀请担任不同权限。
2. **组织总览**：顶部“待处理事项、锚定异常、披露请求”三个动作入口。中部按事项显示最新证据、当前锚定级别、对方是否查看；右侧仅列需人工决策的异常。点击直接到相应记录。
3. **事项与事件轨道**：左侧事项列表；中部按发生/接收顺序切换，但证明顺序只按接收位置；右侧细节显示签名、批次、来源、披露与更正。每一层可展开，不默认把 64 位哈希铺满页面。
4. **提交证据**：单一流程页，先选事项和证据类型，再文字/文件，再隐私与摘要预览，最后确认提交。接收后显示可保存的证明包与“不上传原件”的明确状态。移动端拍照/选文件路径完整。
5. **来源/API**：源列表和健康状态；创建凭证的权限勾选与一次显示；代码样例、幂等请求示例、实时测试沙箱、Webhook 重试记录。不要在 UI 里给机器源伪造一条“手工机器事件”。
6. **外部验证工作台**：首屏显示授权范围和一个明确的分层结果：身份/收据/顺序/锚定/内容。时间线过滤证据类型，点记录展示证明路径和核验动作；内容请求与异议各有独立入口。
7. **披露审批**：管理员看谁要什么、为什么、期限、是否涉及隐私；批准可限定单条/字段/有效期，拒绝给理由，所有动作写审计。先保证最小披露，别把整份 PDF 无条件交出去。
8. **账单与用量**：组织看到合同上限、剩余量、预计超额处理方式和链上紧急锚定次数；验证者不被引导进付款页面。

小交互：保存草稿不产生证据；提交须二次确认；所有异步状态保留可解释历史。校验错误就地展示并聚焦；用户名可用性检查失败时明确是网络/服务故障、可重试，绝不把它当“用户名已被占用”或继续声称已检查。表单服务端仍必须最终做唯一性校验。PWA 可安装；离线时只允许本地草稿/本地哈希，绝不显示“已签收”或“已上链”。

## 6. 主动响应：让系统追着问题跑

“响应”不是自动把所有事写上链。Outdock 要对**证据链失效、业务缺口和验证者需求**主动给出明确动作：

- 事件到达立即签收并推送 UI 状态；链上确认为异步，采用 SSE/短轮询/Webhook，不承诺低于实际链确认时间。
- 源序号中断、重复、迟到、时间倒退、签名不匹配、批次重试、未按 SLA 锚定时创建异常任务并通知具体责任人；去重，避免每条记录发一封邮件。
- 业务模板可设置“交付后 2 小时无签收”之类缺口规则；这是缺少记录的提醒，不是证明交付未发生。
- 验证者提出披露或争议时进入有状态队列，组织收到限时提醒；拒绝/过期同样可见，不能无声消失。
- 需要立即链上证明时由有权限者发起“优先锚定”，记录成本/配额、排队、批次覆盖范围；同一事项跨多批要追踪到**全部目标事件**完成，不能首批成功就标完成。
- 可设置信任监控：收据签名密钥轮换、链 RPC 不可用、合约异常、Webhook 死信、存储/披露密钥错误；运营有运行手册与降级显示。
- 不用 LLM 给“真实性评分”。可用规则帮助找缺口，但用户能看见规则、原始证据与误报申诉。

## 7. API 契约（提案，非现有路由）

统一 `/api/v1`，JSON 错误含 `code`, `message`, `request_id`, 可重试提示。用户网页登录使用 HttpOnly 安全 cookie + CSRF；机器使用分源短权限凭证，不在 URL。所有写入幂等，时间统一 UTC ISO 8601，分页游标，版本化规范。API/SDK 与 UI 共用领域服务，不允许两套不一致的核验算法。

| 路由组 | 作用 | 典型权限 |
|---|---|---|
| `POST/GET /organizations`, `/members`, `/invitations` | 创建组织、成员/邀请管理 | 管理员 |
| `POST/GET /projects`, `/evidence-types`, `/cases` | 模板与事项 | 提交者/管理员 |
| `POST /sources`, `/sources/{id}/credentials`, `/sources/{id}/health` | 来源/凭证/健康 | 集成者/管理员 |
| `POST /cases/{id}/events`, `POST /events:batch` | 人工或机器追加不可变事件 | 对应源 `event:write` |
| `GET /cases/{id}/timeline`, `/events/{id}`, `/receipts/{id}` | 事实与收据读取 | 所属或授权只读 |
| `POST /events/{id}/corrections` | 追加纠错，不覆盖 | 受限写入 |
| `POST /grants`, `GET/DELETE /grants/{id}` | 邀请、授权和撤销 | 管理员 |
| `POST /disclosures/requests`, `/approve`, `/deny` | 最小披露审批 | 验证者请求、管理员决策 |
| `POST /verify`, `GET /proofs/{id}` | 核验包和结构证明 | 公开/授权按范围 |
| `POST /anchors/priority`, `GET /anchors/{batchId}` | 优先锚定与进度 | 限权、可计费 |
| `POST/GET /webhooks`, `/webhook-deliveries` | 回调订阅/重试审计 | 集成者 |

写入示意：`POST /api/v1/cases/{id}/events` 带 `Idempotency-Key`，体含 `source_id`, `evidence_type`, `occurred_at`, `commitment`, `manifest_hash`, `context`, `source_sequence`；响应含 `event_id`, `position`, `received_at`, `receipt`, `anchor_status: pending`。禁止将未授权原文塞进 `context`；限制可索引字段并按模板白名单验证。大批量接入另设异步作业和逐条结果，不能把数千条塞进同步请求。

## 8. 密码学、隐私、链和信任边界

保留旧 OD 中有价值的模式：每记录随机盐、域分隔、规范化 manifest、串行链位置、平台 Ed25519 收据、Merkle 批次、公开链锚点、幂等提交。新设计需固定**规范版本**并提供官方浏览器/Node 参考实现与已知答案测试向量；否则 UI 和 API 计算出不同 C。签名公钥要有版本、轮换/撤销历史与可信登记路径。核验必须比对指定链 ID + 合约代码/地址 + 交易确认，不只信任客户端传来的 RPC 答案。

生产锚定默认 **Base 主网**，开发/测试默认 **Polygon Amoy**；两个环境用不同数据库、密钥、合约和清楚的 UI 标签。链选择是部署配置，不是把 `BASE_*_DEV` 写成 Polygon 而让人误解。链/合约未来可迁移，但每张收据和证明包永远记住当时的版本和目标链。日常按时间/积压阈值聚合；高优先级可触发额外批次。上链仅有批次承诺，不能向验证者提供原件，也不能“解密”H/C。若启用托管，客户端加密、密钥由客户/授权服务控制，服务器端披露必须有审批、范围、审计与可撤销的未来访问；已经交付的明文不可收回。

安全底线：组织级行/权限隔离，最小权限 API 凭证，密钥轮换，速率/大小限制，恶意文件扫描仅在选择托管时触及内容，供应商不可伪装验证者确认，独立校验库可离线运行。对任何“无下载/不可转发”承诺要改成“限权、加水印、审计与合同约束”；浏览器可读的数据无法保证技术上不可复制。

## 9. 建议技术架构与框架选择

**选择：`pnpm create cloudflare@latest my-react-router-app --framework=react-router`。** 这只是一条未来建项目的命令，本任务不执行。2026-09-24 的 Cloudflare 官方文档明确提供该模板、Cloudflare Vite plugin、服务端渲染、Worker 入口、bindings，并允许在 Worker 入口导出 Durable Objects/Workflows；React Router 官方也指向 Cloudflare 模板。出处：[Cloudflare React Router 指南](https://developers.cloudflare.com/workers/framework-guides/web-apps/react-router/)、[React Router 部署指南](https://reactrouter.com/start/framework/deploying)。

为什么适合：ODT 的核心屏幕是登录、组织/事项/事件深链、外部邀请验证与可打印/可分享的只读结果；服务端 loader/action 容易把权限与首屏数据绑定；浏览器端仍可用 Web Crypto 做本地大文件流式哈希（需注意浏览器 Web Crypto 对大型文件的实现，必要时引入经过审核的增量 SHA-256 库/Worker）；Worker 入口可整合 HTTP、Cron、Queues 和 Durable Objects。业务 API 仍应独立于页面 action，实现公开稳定版本和一致鉴权。SEO 只对公开说明页有意义，私有事件页必须 `noindex`。

其他候选：React+Vite 是最轻的 SPA+Worker 组合，但私有深链/SSR/邀请页要自行建立更多路由与数据边界；Astro 更适合内容站；Vue/Svelte 可以实现，却没有本项目既有 React 方向的迁移优势；TanStack Start 也合格但这里没有足够收益抵消换栈与生态迁移；vinext/Next 增加一层兼容面，ODT 没有非要用 Next 功能的理由。此判断是架构取舍，不是性能压测结果。Cloudflare 对 React、Astro、TanStack Start 与 vinext 的说明见 [React+Vite](https://developers.cloudflare.com/workers/framework-guides/web-apps/react/)、[Astro](https://developers.cloudflare.com/workers/framework-guides/web-apps/astro/)、[TanStack Start](https://developers.cloudflare.com/workers/framework-guides/web-apps/tanstack-start/)、[Next/vinext](https://developers.cloudflare.com/workers/framework-guides/web-apps/nextjs/)。

组件建议：React Router Worker（页与 API）→ D1（组织、事项、事件索引、授权、审计）→ 每事项 Durable Object（顺序与头部）→ Queue（通知、Webhooks、批次工作）→ Cron/Workflow（重试、锚定与长任务）→ EVM RPC/合约。若选托管原件才加入 R2 加密对象存储。对百万组织不能假定单一 D1 与单 Worker 部署无限扩容：先以真实写入峰值、热点事项并发、D1 读写、DO 数量、批次大小、gas 与通知速率压测，再决定分片/多租户边界。估算试点为 10–30 家付费组织、每家 1–20 万事件/月，是**容量设计假设**，不是用户预测或定价依据。

## 10. 数据表与状态机（设计级）

最低表集合：`organizations`, `users`, `memberships`, `projects`, `evidence_type_versions`, `cases`, `sources`, `source_credentials`, `events`, `event_receipts`, `idempotency_results`, `anchor_batches`, `anchor_leaves`, `access_grants`, `disclosure_requests`, `disclosure_artifacts`（仅启用托管时）, `webhook_subscriptions`, `webhook_deliveries`, `audit_events`, `usage_counters`。所有业务表带 `org_id` 或明确不可伪造的所有权关联；事件有 `(org_id, case_id, position)` 唯一键与 `(source_id, source_sequence)` 策略约束；幂等键按凭证/路由作用域去重；收据签名材料不可原地修改。删除账户/法定删除请求与不可变承诺的冲突，需在正式上线前完成隐私法和留存政策评估。

事件状态：`draft(local)` → `accepted_signed` → `queued_for_anchor` → `submitted` → `confirming` → `anchored`；失败进入 `retrying` 或 `failed_action_required`，不倒退为“未提交”。更正是一个新事件，不是状态转回草稿。披露请求：`requested` → `approved_scope` / `denied` / `expired` → `material_delivered` → `verified` / `mismatch` / `partial`。显示收据层、链层、内容层三个独立结果；绝不合并成单一模糊绿色勾号。

## 11. 视觉设计稿应覆盖的页面

本次另外生成四张概念 UI 图：

1. [组织事项总览与异常](odt-designs/01-organization-overview.png)
2. [人工提交与本地哈希](odt-designs/02-evidence-submission.png)
3. [外部验证工作台](odt-designs/03-verifier-workbench.png)
4. [来源/API 健康与调用](odt-designs/04-sources-api-health.png)

它们是**设计方向图**，不是可运行 UI、也不证明已实现。图中文字和示例数据不构成 API 契约：尤其提交页的“已签名”只应在提交成功后出现；来源页的代码片段应以本文件第 7 节的承诺字段与脱敏规则为准；验证页须以 Outdock 已注册公钥而非提交组织自行宣称的签名者为准。统一亮色：暖白底、深墨正文、陶土色主操作、灰绿状态辅助；桌面紧凑但不拥挤，13 英寸无需横向滚动；平板叠合右侧细节，翻盖手机窄屏单列并保留核心状态标签。关键“证据层级”必须在不同页面同名、同顺序。

## 12. 实施分期、验收与停止条件

**Phase 0：需求验证（先于大开发）**。找 5–10 个有实际争议/对账成本的组织，收集去标识化样本流程；问谁提交、谁核、现在花多久、争议损失、是否愿让外部伙伴用一个新链接。做一份手工证明包与核验演示。通过标准：至少 3 家能指出真实近期事项并同意试点；不能把“感觉有用”算付费意向。

**Phase 1：可信最小闭环**。一个组织、一个合作方、一个事项模板、网页提交 + API 写入、即时签名收据、每日链锚定、外部邀请、独立分层核验。先不做通用行业模板市场、AI 裁决、托管原件、复杂计费。验收：重复请求不重复写；并发位置不冲突；原件改变 1 字节内容校验失败；错误链/合约/签名被拒；待锚定不显示已上链；验证者无法跨组织读取。

**Phase 2：真正可运营**。来源健康与异常、Webhook 重试、优先锚定且全范围完成、披露请求/批准/审计、加密托管的可行性测试、组织成员权限、灾备/密钥轮换。验收含高并发热点事项、RPC 中断、部分批量成功、邮件/回调故障、跨环境隔离、移动设备完整流程。

**Phase 3：行业扩展**。只有在试点证实复用价值后加行业模板、深度集成、客户自助配置与收费。衡量：从事件发生到可独立核验的时长、异常被处理率、对账工时、验证者完成率、复用率、付费续约与单位经济；不用“链上交易数”代替客户价值。

**停止/收缩条件**：组织没有可量化的争议或合规成本；外部伙伴拒绝使用/无法取得原件与盐；记录提交高度依赖人工重复输入；客户现有电子签/审计系统已充分解决同一问题；链上锚定费用和运维风险高于交付价值。可退回为更小的“签名收据 + 独立核验 API”，不用为区块链叙事维持复杂性。

## 13. 与旧 OD 的关系（必须保留的记忆）

旧 OD 的 H（人用 UI + API）、M（机器 API 写、UI 读）、付费 Verifier、四档 Supplier 套餐、$25/7 天与 $88/28 天、Polygon Amoy dev/Base prod，以及已有的密码学/证明设计，在 `membrane/` 中作为**历史方案**保留。本提案重组账户为组织与成员、把 H/M 变成来源/操作方式、建议组织付费而受邀验证者免费，并扩大为跨组织“事实交接”基础设施。它不是对历史代码已完成的声明，也不是要求现在删改旧文档。若未来决定实施，应先写迁移/兼容性计划，明确旧收据和合约仍可被验证，旧客户授权不会被静默改变。
